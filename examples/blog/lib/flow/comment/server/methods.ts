// @generated by next-prisma-flow-state-engine
// file: server/methods.ts

"use server";

import { prisma } from "../../prisma";
import type { Prisma } from "../../prisma";
import { FlowCtx, FlowPolicyError } from "../../core";
import { canComment } from "../../policies";
import { CommentSelect } from "./selects";

export async function findUnique(
  args: Prisma.CommentFindUniqueArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("read", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.findUnique({
    select: CommentSelect,
    ...args,
    where: { ...args.where, ...policy.where },
  });
}

export async function findUniqueOrThrow(
  args: Prisma.CommentFindUniqueOrThrowArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("read", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.findUniqueOrThrow({
    select: CommentSelect,
    ...args,
    where: { ...args.where, ...policy.where },
  });
}

export async function findFirst(
  args?: Prisma.CommentFindFirstArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("list", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.findFirst({
    select: CommentSelect,
    ...args,
    where: { ...args?.where, ...policy.where },
  });
}

export async function findFirstOrThrow(
  args?: Prisma.CommentFindFirstOrThrowArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("list", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.findFirstOrThrow({
    select: CommentSelect,
    ...args,
    where: { ...args?.where, ...policy.where },
  });
}

export async function findMany(
  args?: Prisma.CommentFindManyArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("list", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.findMany({
    select: CommentSelect,
    ...args,
    where: { ...args?.where, ...policy.where },
  });
}

export async function create(
  args: Prisma.CommentCreateArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("create", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  const result = await prisma.comment.create({
    select: CommentSelect,
    ...args,
    data: { ...args.data, ...policy.data },
  });

  return result;
}

export async function createMany(
  args: Prisma.CommentCreateManyArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("create", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  // Apply policy data to each item
  const data = Array.isArray(args.data)
    ? args.data.map((item) => ({ ...item, ...policy.data }))
    : { ...args.data, ...policy.data };

  return prisma.comment.createMany({
    ...args,
    data,
  });
}

export async function update(
  args: Prisma.CommentUpdateArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("update", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  const result = await prisma.comment.update({
    select: CommentSelect,
    ...args,
    where: { ...args.where, ...policy.where },
    data: { ...args.data, ...policy.data },
  });

  return result;
}

export async function updateMany(
  args: Prisma.CommentUpdateManyArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("update", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.updateMany({
    ...args,
    where: { ...args.where, ...policy.where },
    data: { ...args.data, ...policy.data },
  });
}

export async function upsert(
  args: Prisma.CommentUpsertArgs,
  ctx: FlowCtx = {},
) {
  // Check both create and update policies
  const createPolicy = await canComment("create", ctx);
  const updatePolicy = await canComment("update", ctx);

  // Need both permissions for upsert
  if (!createPolicy.ok || !updatePolicy.ok) {
    throw new FlowPolicyError("Insufficient permissions for upsert");
  }

  return prisma.comment.upsert({
    select: CommentSelect,
    ...args,
    where: { ...args.where, ...updatePolicy.where },
    create: { ...args.create, ...createPolicy.data },
    update: { ...args.update, ...updatePolicy.data },
  });
}

export async function deleteOne(
  args: Prisma.CommentDeleteArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("delete", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.delete({
    ...args,
    where: { ...args.where, ...policy.where },
  });
}

export async function deleteMany(
  args?: Prisma.CommentDeleteManyArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("delete", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.deleteMany({
    ...args,
    where: { ...args?.where, ...policy.where },
  });
}

export async function count(args?: Prisma.CommentCountArgs, ctx: FlowCtx = {}) {
  const policy = await canComment("list", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.count({
    ...args,
    where: { ...args?.where, ...policy.where },
  });
}

export async function aggregate(
  args: Prisma.CommentAggregateArgs,
  ctx: FlowCtx = {},
) {
  const policy = await canComment("list", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.aggregate({
    ...args,
    where: { ...args.where, ...policy.where },
  });
}

export async function groupBy<
  T extends Prisma.CommentGroupByArgs,
  HasSelectOrTake extends Prisma.Or<
    Prisma.Extends<"skip", Prisma.Keys<T>>,
    Prisma.Extends<"take", Prisma.Keys<T>>
  >,
  OrderByArg extends Prisma.True extends HasSelectOrTake
    ? { orderBy: Prisma.CommentGroupByArgs["orderBy"] }
    : { orderBy?: Prisma.CommentGroupByArgs["orderBy"] },
  OrderFields extends Prisma.ExcludeUnderscoreKeys<
    Prisma.Keys<Prisma.MaybeTupleToUnion<T["orderBy"]>>
  >,
  ByFields extends Prisma.MaybeTupleToUnion<T["by"]>,
  ByValid extends Prisma.Has<ByFields, OrderFields>,
  HavingFields extends Prisma.GetHavingFields<T["having"]>,
  HavingValid extends Prisma.Has<ByFields, HavingFields>,
  ByEmpty extends T["by"] extends never[] ? Prisma.True : Prisma.False,
  InputErrors extends ByEmpty extends Prisma.True
    ? `Error: "by" must not be empty.`
    : HavingValid extends Prisma.False
      ? {
          [P in HavingFields]: P extends ByFields
            ? never
            : P extends string
              ? `Error: Field "${P}" used in "having" needs to be provided in "by".`
              : [
                  Error,
                  "Field ",
                  P,
                  ` used in "having" needs to be provided in "by"`,
                ];
        }[HavingFields]
      : "take" extends Prisma.Keys<T>
        ? "orderBy" extends Prisma.Keys<T>
          ? ByValid extends Prisma.True
            ? {}
            : {
                [P in OrderFields]: P extends ByFields
                  ? never
                  : `Error: Field "${P}" in "orderBy" needs to be provided in "by"`;
              }[OrderFields]
          : 'Error: If you provide "take", you also need to provide "orderBy"'
        : "skip" extends Prisma.Keys<T>
          ? "orderBy" extends Prisma.Keys<T>
            ? ByValid extends Prisma.True
              ? {}
              : {
                  [P in OrderFields]: P extends ByFields
                    ? never
                    : `Error: Field "${P}" in "orderBy" needs to be provided in "by"`;
                }[OrderFields]
            : 'Error: If you provide "skip", you also need to provide "orderBy"'
          : ByValid extends Prisma.True
            ? {}
            : {
                [P in OrderFields]: P extends ByFields
                  ? never
                  : `Error: Field "${P}" in "orderBy" needs to be provided in "by"`;
              }[OrderFields],
>(
  args: Prisma.SubsetIntersection<T, Prisma.CommentGroupByArgs, OrderByArg> &
    InputErrors,
  ctx: FlowCtx = {},
): Promise<
  {} extends InputErrors ? Prisma.GetCommentGroupByPayload<T> : InputErrors
> {
  const policy = await canComment("list", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  return prisma.comment.groupBy({
    ...(args as any),
    where: { ...args.where, ...policy.where },
  } as any) as any;
}
