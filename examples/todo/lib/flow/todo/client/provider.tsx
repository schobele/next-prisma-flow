// @generated by next-prisma-flow-state-engine
// file: client/provider.tsx

"use client";

import { type ReactNode, useEffect, useMemo, useCallback, useRef } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import {
  FlowFormProvider,
  useFlowFormContext,
  type FlowFormFeatures,
  type FlowFormMode,
} from "../../core/form-provider";
import { TodoCreateSchema, TodoUpdateSchema } from "../types/schemas";
import type {
  FlowTodo,
  FlowTodoCreate,
  FlowTodoUpdate,
} from "../types/schemas";
import { useTodo, useCreateTodo, useUpdateTodo } from "./hooks";

/**
 * Props for the Todo form component
 */
export interface FlowTodoFormProps {
  children: ReactNode;

  /**
   * Form mode - determines behavior and data handling
   * - "create": New entity creation
   * - "update": Edit existing entity (requires id)
   * - "view": Read-only display
   */
  mode: FlowFormMode;

  /**
   * Entity ID for update mode
   */
  id?: string;

  /**
   * Default values for form fields
   */
  defaultValues?: Partial<FlowTodoCreate | FlowTodoUpdate>;

  /**
   * Callback when form is successfully submitted
   */
  onSuccess?: (data: FlowTodo) => void | Promise<void>;

  /**
   * Callback when form submission fails
   */
  onError?: (error: Error) => void;

  /**
   * Form features configuration
   */
  features?: FlowFormFeatures;

  /**
   * Additional form element props
   */
  className?: string;
  style?: React.CSSProperties;
}

/**
 * Extract scalar fields from entity (removes relations)
 */
function extractScalarFields(data: any): any {
  if (!data) return {};
  const result: any = {};
  Object.keys(data).forEach((key) => {
    const value = data[key];
    if (value === null || value === undefined) {
      result[key] = value;
    } else if (typeof value !== "object" || value instanceof Date) {
      result[key] = value;
    } else if (!Array.isArray(value) && !value.id) {
      // Include simple objects without id (like JSON fields)
      result[key] = value;
    }
  });
  return result;
}

/**
 * Type-safe form component for Todo
 *
 * @example
 * ```tsx
 * <FlowTodoForm mode="create">
 *   <TodoFormField
 *     name="title"
 *     render={({ field }) => (
 *       <FormItem>
 *         <FormLabel>Title</FormLabel>
 *         <FormControl>
 *           <Input {...field} />
 *         </FormControl>
 *         <FormMessage />
 *       </FormItem>
 *     )}
 *   />
 *   <Button type="submit">Submit</Button>
 * </FlowTodoForm>
 * ```
 */
export function FlowTodoForm({
  children,
  mode,
  id,
  defaultValues,
  onSuccess,
  onError,
  features,
  className,
  style,
}: FlowTodoFormProps) {
  // Validate props
  if (mode === "update" && !id) {
    throw new Error(`FlowTodoForm: id is required when mode is "update"`);
  }

  // Fetch existing data for update mode
  const { data: existingData, isLoading: isLoadingData } = useTodo(id || "", {
    enabled: mode === "update" && !!id,
  });

  // Initialize form with proper schema based on mode
  const form = useForm<FlowTodoCreate | FlowTodoUpdate>({
    resolver: zodResolver(
      mode === "create" ? TodoCreateSchema : TodoUpdateSchema,
    ),
    defaultValues: defaultValues as any,
    mode: "onChange", // Enable real-time validation
  });

  // Track if we've loaded existing data
  const hasLoadedData = useRef(false);

  // Update form when existing data loads (for update mode)
  useEffect(() => {
    if (mode === "update" && existingData && !hasLoadedData.current) {
      const scalarData = extractScalarFields(existingData);
      form.reset(scalarData, {
        keepDefaultValues: false,
      });
      hasLoadedData.current = true;
    }
  }, [mode, existingData, form]);

  // Setup mutations
  const createMutation = useCreateTodo({
    onSuccess: async (data) => {
      form.reset();
      hasLoadedData.current = false;
      await onSuccess?.(data);
    },
    onError,
  });

  const updateMutation = useUpdateTodo(id!, {
    onSuccess: async (data) => {
      // Reset form with new data to clear dirty state
      const scalarData = extractScalarFields(data);
      form.reset(scalarData, {
        keepDefaultValues: false,
      });
      await onSuccess?.(data);
    },
    onError,
  });

  // Form submission handler
  const handleSubmit = useCallback(
    async (data: FlowTodoCreate | FlowTodoUpdate) => {
      try {
        if (mode === "create") {
          await createMutation.mutateAsync(data as FlowTodoCreate);
        } else if (mode === "update") {
          await updateMutation.mutateAsync(data as FlowTodoUpdate);
        }
      } catch (error) {
        // Error is already handled by mutation callbacks
        console.error("Form submission error:", error);
      }
    },
    [mode, createMutation, updateMutation],
  );

  // Autosave handler (for partial field updates)
  const handleAutosave = useCallback(
    async (data: Partial<FlowTodoUpdate>) => {
      if (mode === "update" && id) {
        try {
          await updateMutation.mutateAsync(data as FlowTodoUpdate);
        } catch (error) {
          console.error("Autosave error:", error);
          throw error;
        }
      }
    },
    [mode, id, updateMutation],
  );

  // Show loading state while fetching data in update mode
  if (mode === "update" && isLoadingData) {
    return (
      <div className={className} style={style}>
        <div className="flex items-center justify-center p-8">
          <span>Loading...</span>
        </div>
      </div>
    );
  }

  return (
    <form
      className={className}
      style={style}
      onSubmit={(e) => {
        e.preventDefault();
        e.stopPropagation();
      }}
    >
      <FlowFormProvider
        form={form as any}
        mode={mode}
        modelName="Todo"
        features={features}
        onSubmit={handleSubmit}
        onSave={features?.autosave ? handleAutosave : undefined}
      >
        {children}
      </FlowFormProvider>
    </form>
  );
}

/**
 * Submit button that automatically shows loading state
 */
export function FlowTodoFormSubmit({
  children = "Submit",
  className,
  ...props
}: React.ButtonHTMLAttributes<HTMLButtonElement>) {
  const { submit, isSubmitting } = useFlowFormContext();

  return (
    <button
      type="button"
      onClick={submit}
      disabled={isSubmitting}
      className={className}
      {...props}
    >
      {isSubmitting ? "Submitting..." : children}
    </button>
  );
}

/**
 * Reset button to clear form
 */
export function FlowTodoFormReset({
  children = "Reset",
  className,
  ...props
}: React.ButtonHTMLAttributes<HTMLButtonElement>) {
  const { reset } = useFlowFormContext();

  return (
    <button type="button" onClick={reset} className={className} {...props}>
      {children}
    </button>
  );
}

/**
 * Form state indicator
 */
export function FlowTodoFormState({ className }: { className?: string }) {
  const { hasUnsavedChanges, isSaving, autosave } = useFlowFormContext();

  if (!hasUnsavedChanges && !isSaving) {
    return null;
  }

  return (
    <div className={className}>
      {isSaving && <span>Saving...</span>}
      {!isSaving && hasUnsavedChanges && <span>Unsaved changes</span>}
      {autosave?.lastSaved && (
        <span>Last saved: {autosave.lastSaved.toLocaleTimeString()}</span>
      )}
    </div>
  );
}
