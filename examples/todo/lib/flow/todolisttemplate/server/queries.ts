// @generated by next-prisma-flow-state-engine
// file: server/queries.ts

"use server";

import { prisma } from "../../prisma";
import { cacheTagged, FlowCtx, FlowPolicyError } from "../../core";
import { canTodoListTemplate } from "../../policies";
import { TodoListTemplateSelect } from "./selects";
import type { Prisma } from "../../prisma";
import type { FlowTodoListTemplate } from "../types/schemas";

// Transform Prisma response to match FlowTodoListTemplate schema (null -> undefined for relations)
function transformResponse(item: any): any {
  if (!item) return item;
  const result = { ...item };
  return result;
}

function transformResponseList(items: any[]): any[] {
  return items.map(transformResponse);
}

export const getTodoListTemplateById = cacheTagged(async function (
  id: string,
  ctx: FlowCtx = {},
): Promise<FlowTodoListTemplate | null> {
  const policy = await canTodoListTemplate("read", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  const item = (await prisma.todoListTemplate.findUnique({
    where: { id: id, ...policy.where },
    select: TodoListTemplateSelect,
  })) as FlowTodoListTemplate | null;

  return transformResponse(item) as FlowTodoListTemplate | null;
});

export type TodoListTemplateListParams = {
  where?: Prisma.TodoListTemplateWhereInput;
  orderBy?:
    | Prisma.TodoListTemplateOrderByWithRelationInput
    | Prisma.TodoListTemplateOrderByWithRelationInput[];
  skip?: number;
  take?: number;
  cursor?: Prisma.TodoListTemplateWhereUniqueInput;
};

export const listTodoListTemplates = cacheTagged(async function (
  params: TodoListTemplateListParams = {},
  ctx: FlowCtx = {},
): Promise<{ items: FlowTodoListTemplate[]; total: number }> {
  const policy = await canTodoListTemplate("list", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  const where = { ...params.where, ...policy.where };

  const [items, total] = await Promise.all([
    prisma.todoListTemplate.findMany({
      where,
      orderBy: params.orderBy,
      skip: params.skip,
      take: params.take || 50,
      cursor: params.cursor,
      select: TodoListTemplateSelect,
    }) as Promise<FlowTodoListTemplate[]>,
    prisma.todoListTemplate.count({ where }),
  ]);

  return {
    items: transformResponseList(items) as FlowTodoListTemplate[],
    total,
  };
});

export const searchTodoListTemplates = cacheTagged(async function (
  query: string,
  params: Omit<TodoListTemplateListParams, "where"> = {},
  ctx: FlowCtx = {},
): Promise<{ items: FlowTodoListTemplate[]; total: number }> {
  const policy = await canTodoListTemplate("list", ctx || {});
  if (!policy.ok) throw new FlowPolicyError(policy.message);

  const where = {
    AND: [
      policy.where,
      {
        OR: [{ name: { contains: query, mode: "insensitive" as const } }],
      },
    ],
  };

  const [items, total] = await Promise.all([
    prisma.todoListTemplate.findMany({
      where,
      orderBy: params.orderBy,
      skip: params.skip,
      take: params.take || 50,
      cursor: params.cursor,
      select: TodoListTemplateSelect,
    }) as Promise<FlowTodoListTemplate[]>,
    prisma.todoListTemplate.count({ where }),
  ]);

  return {
    items: transformResponseList(items) as FlowTodoListTemplate[],
    total,
  };
});
