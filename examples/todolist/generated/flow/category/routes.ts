// This file is auto-generated by Next Prisma Flow Generator.
// Do not edit this file manually as it will be overwritten.
// Generated at: 2025-06-02T18:57:26.036Z

import { type NextRequest, NextResponse } from 'next/server';
import * as CategoryActions from './actions';

async function GET(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
    const id = searchParams.get('id');
    
    if (id) {
      const result = await CategoryActions.getCategory(id);
      if (!result) {
        return NextResponse.json(
          { error: 'Category not found' },
          { status: 404 }
        );
      }
      return NextResponse.json(result);
    } else {
      const results = await CategoryActions.getAllCategories();
      return NextResponse.json(results);
    }
  } catch (error) {
    console.error('GET /category error:', error);
    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}

async function POST(request: NextRequest) {
  try {
    const data = await request.json();
    const result = await CategoryActions.createCategory(data);
    return NextResponse.json(result, { status: 201 });
  } catch (error) {
    console.error('POST /category error:', error);
    const status = (error as any)?.code === 'P2002' ? 409 : 400;
    return NextResponse.json(
      { error: error instanceof Error ? error.message : 'Invalid request' },
      { status }
    );
  }
}

async function PATCH(request: NextRequest) {
  try {
    const data = await request.json();
    const { id, ...updateData } = data;
    
    if (!id) {
      return NextResponse.json(
        { error: 'Missing id in request body' },
        { status: 400 }
      );
    }
    
    const result = await CategoryActions.updateCategory(id, updateData);
    return NextResponse.json(result);
  } catch (error) {
    console.error('PATCH /category error:', error);
    let status = 400;
    if ((error as any)?.code === 'P2025') status = 404;
    if ((error as any)?.code === 'P2002') status = 409;
    
    return NextResponse.json(
      { error: error instanceof Error ? error.message : 'Update failed' },
      { status }
    );
  }
}

async function DELETE(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
    const id = searchParams.get('id');
    
    if (!id) {
      return NextResponse.json(
        { error: 'Missing id parameter' },
        { status: 400 }
      );
    }
    
    await CategoryActions.deleteCategory(id);
    return NextResponse.json({ success: true });
  } catch (error) {
    console.error('DELETE /category error:', error);
    const status = (error as any)?.code === 'P2025' ? 404 : 500;
    return NextResponse.json(
      { error: error instanceof Error ? error.message : 'Delete failed' },
      { status }
    );
  }
}

export const routesHandlers = {
	GET,
	POST,
	PATCH,
	DELETE,
};
